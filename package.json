{
  "name": "ic-frontend-canister-manager",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "echo 'Skipping build step...'",
    "task:copy-files": "cp -r custom-domain-files/. dist/",
    "deploy:test": "cd <MOTOKO_CRAFTING_TABLE_PATH> && npm run deploy:frontend1",
    "deploy:prod": "npm run task:copy-files && dfx deploy <CANISTER_NAME> --ic"
  }
}
